num_mines = 5

cell_num = 1

# create grid
num_rows = 5
num_columns = 5


class Cell:
    def __init__(self, mine, num, clicked):
        self.mine = False
        self.num = 0
        self.clicked = False

for row in range(num_rows):
    for cell in range(num_columns):
        (cell + str(cell_num)) = Cell.(True, 0, False)
        print(str(cell + str(cell_num)))
        cell_num += 1



if a1 in list:
